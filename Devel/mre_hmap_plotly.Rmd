
```{r}
library(ggplot2)
library(d3heatmap)
library(plotly)

m <- matrix(1:25,nrow = 5,ncol = 5,dimnames = list(LETTERS[1:5],letters[1:5]))
df <- data.frame(m, stringsAsFactors = FALSE)

p <- ggplot(df,aes(x=a, y=b))+
  geom_point()
```

```{r}
d3heatmap(df)

```



```{r}
ggplotly(p)
```



