##Normalization

Each signal of the dataset was independently normalized between arrays using the RUV3 method with the array as the unit of study and residuals as the negative controls then normalized within array using a loess model of the residuals.